# .env
PORT=5001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Use your macOS username for simplicity
DB_HOST=localhost
DB_PORT=5432
DB_USER=saidhafizzan
DB_PASSWORD=  # Leave empty if no password (common for local PostgreSQL)
DB_NAME=location_app

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345
JWT_EXPIRE=7d

# File Upload
MAX_FILE_SIZE=5242880
